# MavFix
This project is run on Node.js/React.js v24.21.0.

To run the project after Node.js has been installed run 'npm install' in the project directory.

To run the local server for the website for debugging run 'npm run dev' in the project directory.
